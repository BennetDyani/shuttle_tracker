import 'package:flutter/material.dart';

class ReportMaintenanceScreen extends StatelessWidget {
  const ReportMaintenanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Report Maintenance'),
      ),
      body: const Center(
        child: Text('Report Maintenance Screen - Under Development'),
      ),
    );
  }
}
